# Tầng dữ liệu Research SPY. Cài thẳng vào Python của máy (không dùng venv):
#   python -m pip install -r requirements.txt
#   python -m playwright install chromium
#
# playwright ghim ở 1.56.0 vì bản đó kéo theo pyee==13.0.1 — đúng bản mà TikTokLive đang
# cài sẵn trên máy cần. Hạ playwright xuống sẽ hạ luôn pyee và làm hỏng gói đó.
# fastapi 0.138.0 (nâng từ 0.115.6, 2026-09-03) để kéo starlette>=0.46.0 — bản 0.41.3 cũ dính
# 10 CVE (DoS/multipart, PYSEC-2026-161/248/249/1941/1942/2280/2281). Đây đúng là bộ đang chạy
# trên máy dev (fastapi 0.138 + starlette 1.3.1 + pydantic 2.11.9), `import app.main` OK. fastapi
# 0.138 chỉ cần pydantic>=2.9.0 nên pin 2.10.5 dưới đây vẫn hợp lệ.
fastapi==0.138.0
starlette>=1.3.1
# python-multipart: BẮT BUỘC vì app có endpoint nhận Form/File (upload ảnh). fastapi 0.115.6
# cũ chỉ kiểm gói này lúc GỌI request nên thiếu vẫn import được; fastapi 0.138 kiểm ngay lúc
# đăng ký route (import app.main) → thiếu là backend không lên. Máy dev vốn có sẵn nên trước
# đây không lộ; env sạch (CI/VPS) thì phải khai tường minh.
python-multipart==0.0.32
uvicorn[standard]==0.34.0
httpx==0.28.1
playwright==1.56.0
pydantic==2.10.5
# python-dotenv 1.2.3 (nâng từ 1.0.1) — 1.0.1 dính PYSEC-2026-2270, vá ở 1.2.2.
python-dotenv==1.2.3

# Mục Quảng cáo — khớp sản phẩm bằng ẢNH (lib/ads/imagematch.py, clipmatch.py) và mục Tìm bằng
# ảnh. Hai gói này được import NGAY LÚC KHỞI ĐỘNG, nên thiếu là server không lên được.
numpy==2.4.6
Pillow==12.3.0

# Chạy CLIP image-encoder cho khớp ảnh theo NGỮ NGHĨA (cùng sản phẩm dù khác góc chụp). Đây là
# gói DUY NHẤT trong danh sách này có thể bỏ: thiếu nó thì `clip_available()` trả False và
# luồng tự rơi về pHash (chỉ bắt được ảnh trùng gần như từng pixel). Model không nằm trong
# repo — xem `lib/ads/clipmatch.py` để biết chỗ đặt `backend/models/clip-vision.onnx`.
onnxruntime==1.28.0

# Tài khoản người dùng + phân tích: cả 2 chỉ dùng khi khai SUPABASE_URL. Không khai → module
# tự tắt, giao diện admin/đăng nhập hoạt động ở chế độ chỉ-localStorage như trước. Đây là hai
# gói TỰ CHỌN — quăng khỏi requirements.txt sẽ làm hỏng tính năng nhưng KHÔNG làm hỏng crawl.
#
# supabase>=2.15 để tương thích httpx 0.28.1 (bản cũ 2.9.1 kéo httpx xuống 0.27, làm google-genai
# gãy — đã đo 2026-08-25). Cụ thể đang cài 2.31.0 nhưng đặt trần rộng để pip resolver có chỗ xoay.
supabase>=2.15,<3
PyJWT>=2.10,<3

# ─── Mục Trend Signal Hub (`backend/hub/`) ───────────────────────────────────────
# Đo 2026-08-25 bằng cách chặn từng gói lúc import: gói DUY NHẤT mà `hub` cần ngay lúc nạp
# module là pydantic-settings (hub/config.py). Thiếu nó thì `app/main.py` bắt lỗi, backend
# vẫn lên, và `/api/hub/health` trả về lý do — ba mục còn lại không hề hấn gì.
pydantic-settings==2.7.1

# Bốn gói dưới đây đều được import LƯỜI, bên trong hàm, nên không ghim phiên bản: cứ có bản
# nào trên máy là dùng bản đó. Máy đang chạy pandas 3.0.5 / anthropic 1.0.0 — cao hơn hẳn
# bản mà dự án gốc ghim (2.2.3 / 0.42.0) — và Hub chạy đúng, nên ghim ngược xuống chỉ tổ
# hạ cấp những gói mà phần còn lại của máy đang dùng.
#   anthropic        trợ lý AI trong Hub          — thiếu: Hub chạy heuristic, vẫn ra số
#   beautifulsoup4   cào Amazon (hub/workers)     — thiếu: riêng đường cào Amazon nằm im
#   pytrends         đường dự phòng Google Trends — thiếu: đường chính (gói `gtrends` ở gốc
#                                                   repo, Chrome thật) vẫn chạy
#   pandas           dự án gốc ghim, nhưng không chỗ nào trong `hub/` import tới
anthropic
beautifulsoup4
pytrends
